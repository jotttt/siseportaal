<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">×</span><span class="sr-only">Close</span></button>
    <h4 class="modal-title">Firma muutmine</h4>
</div>

<div class="modal-body">
    <div class="row">
        <div class="col-sm-12">
            <div class="form-group">
                <label>Firma nimi</label>
                <div class="input-group" style="width: 100%;">
                    <select id="company" class="chosen-select" data-placeholder="{{t.txt_search_dot_dot}}">
                        {{#each companies}}
                        <option value="{{this.id}}" {{#if this.selected}}selected{{/if}}>{{this.name}}</option>
                        {{/each}}
                    </select>
                    <a href="#add_organization" id="add_organization" class="btn btn-xs btn-default btn-outline font-bold"><i class="fa fa-plus"></i> Lisa firma</a>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12">
            <div class="form-group">
                <label>Seotus</label>
                <div class="input-group" style="width: 100%;">
                    <select id="relation" class="chosen-select" data-placeholder="{{t.txt_search_dot_dot}}">
                        {{#each relations}}
                        <option value="{{this.id}}" {{#if this.selected}}selected{{/if}}>{{this.name}}</option>
                        {{/each}}
                    </select>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal-footer">
    <div class="row">
        <div class="col-sm-4">
        </div>
        <div class="col-sm-8">
            <button type="button" class="btn btn-white" data-dismiss="modal">Sule</button>
        </div>
    </div>
</div>
